import styled from 'styled-components';

export const Card = styled.div`
  border: 1px solid #c1c1c1;
  padding: 16px;
`;

export const LinkBotao = styled.a`
  color: #fff;
  font-size: 14px;
  background-color: #4476bf; /* corrigido */
  text-decoration: none;
  padding: 8px;
  border-radius: 4px; /* opcional: deixa o botão mais bonito */
  display: inline-block; /* garante que o padding funcione certinho */
  margin-top: 24px;
`;
